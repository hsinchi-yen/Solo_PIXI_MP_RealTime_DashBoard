services:
  dashboard:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./rawlogs:/app/rawlogs:ro
      - ./config:/app/config:ro
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 256M
