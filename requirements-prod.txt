# Production dependencies only — no test/dev tools
# Note: plain uvicorn (no [standard]) avoids uvloop/httptools compilation
# on embedded targets (Yocto, Alpine) where C headers may be absent.
fastapi==0.111.*
uvicorn==0.30.*
watchdog==4.*
tomli>=2.0; python_version < "3.11"
asyncpg>=0.29
psycopg2-binary>=2.9
